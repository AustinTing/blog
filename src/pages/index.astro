---
import BaseLayout from '../layouts/BaseLayout.astro';

const lessons = [
  {
    title: 'Lesson 05 — Background canvas & hero shell',
    description:
      'Compose layered background components, wire a reusable hero shell, and set spacing for future homepage sections.',
  },
  {
    title: 'Lesson 06 — Content collections & schema typing',
    description:
      'Define content collections with schema typing and helper utilities so Markdown/MDX entries gain type-safe frontmatter.',
  },
  {
    title: 'Lesson 07 — Blog list view & post metadata',
    description:
      'Render a blog index from the content collections while reusing shared metadata components for consistent cards.',
  },
];
---
<BaseLayout
  title="Welcome"
  description="A clean starting point for learning Astro step by step."
>
  <section class="hero" aria-labelledby="page-title">
    <h1 id="page-title">Learning Astro Blog</h1>
    <p class="lead">
      Welcome! This workspace starts simple on purpose so we can rebuild the
      <code>astro-antfustyle-theme</code> piece by piece.
    </p>
    <section class="callout" aria-label="Next steps">
      <h2>What we will do next</h2>
      <ol class="next-steps">
        {lessons.map(({ title, description }) => (
          <li>
            <h3>{title}</h3>
            <p>{description}</p>
          </li>
        ))}
      </ol>
    </section>
  </section>
</BaseLayout>
